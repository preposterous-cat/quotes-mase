export const metadata = {
  title: "Kata-kata hari ini mase",
  description: "Generate Quote kata-kata hari ini",
};

export default function MainLayout({ children }) {
  return <main>{children}</main>;
}
